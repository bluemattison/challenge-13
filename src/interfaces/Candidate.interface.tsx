// TODO: Create an interface for the Candidate objects returned by the API
export interface Candidate {
    name: string;
    login: string;
    email: string;
    location: string;
    avatar_url: string;
    html_url: string;
    company: string;
}

// I forgot how to pull from the API, so im just filling this in and what the todos say. :( 
// I cant figure it out so im just turning stuff in to get some points and then come back and fill in the rest later.
// Thanks for grading. :)